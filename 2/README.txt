3. Write a web page that contains images of people in a table.
The table should be formatted using a stylesheet.
Whenever the mouse hovers over an image of a person,
    a div containing information about that person should pop up.

16. Write a web page that contains a vertical menu.
The menu should be displayed in the left side of the page
    and should contain at least 5 items (displayed vertically,
    one on top of the other). Each item should expand to the
    right containing a group of at least 5 commands.
